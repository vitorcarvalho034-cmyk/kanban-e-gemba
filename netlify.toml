[build]
  command = "pnpm install && cd client && pnpm build"
  publish = "client/dist"

[dev]
  command = "pnpm dev"
  port = 3000

# SPA routing - redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
